name: Deploy

on:
  push:
    branches: [ master ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: LeLuxNet/GitPullApi-GitHub-Action@master
      with:
        repository: website
        token: ${{ secrets.Token }}
