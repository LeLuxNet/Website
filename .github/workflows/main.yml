name: Test

on:
  push:
    branches: [ master ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: LeLuxNet/GitPullApi-GitHub-Action@master
      with:
        repository: website
        token: ${{ secrets.Token }}
